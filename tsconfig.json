{
  "compilerOptions": {
    "target": "ES2020",                       /* Specify ECMAScript target version: 'ES3' (default), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019', 'ES2020', or 'ESNEXT'. */
    "outDir": "app/assets/js",                /* Local onde o resultado da transpilacao do typecript para javascript sera armazenado. */
    "removeComments": true,                   /* Instrucao para que todos os comentarios inseridos nos fontes typescript sejam retirados no momento de gerar o javascript. */
    "noEmitOnError": true,                    /* Em caso de erro nao gera o js. */
    "strict": true,                           /* Enable all strict type-checking options. */
    "noImplicitAny": true,                    /* Forcar tipagem das Raise error on expressions and declarations with an implied 'any' type. */
    "skipLibCheck": true,                     /* Skip type checking of declaration files. */
    "forceConsistentCasingInFileNames": true  /* Disallow inconsistently-cased references to the same file. */
  },
  "include": [
    "app/ts/**/*"
  ]
}
